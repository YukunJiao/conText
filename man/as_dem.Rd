% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_dem.R
\name{as_dem}
\alias{as_dem}
\title{Wrap a matrix of embeddings as a \code{dem} (document-embedding matrix)}
\usage{
as_dem(x, docvars = NULL, features = character(0))
}
\arguments{
\item{x}{a (numeric) N x D matrix of embeddings: one row per instance
(document/context), D embedding dimensions. Row names, if present, are used as
document ids.}

\item{docvars}{a \code{data.frame} of document variables with one row per row of
\code{x} (same order). If \code{NULL} (default), an empty set of docvars is used.}

\item{features}{(character) optional vector of features used to compute the
embeddings (stored for reference only).}
}
\value{
a \code{dem-class} object.
}
\description{
Constructs a \code{dem-class} object from an existing matrix of per-instance
embeddings and (optionally) their document variables. This lets you use
embeddings produced outside of conText -- e.g. contextual embeddings from a
transformer (BERT/RoBERTa/ModernBERT), decontextualized static embeddings, or
any other per-instance representation -- with conText's downstream tooling,
including the embedding regression in \code{\link[=conText]{conText()}} (pass the resulting \code{dem} as
\code{data} with a \code{.} on the left-hand side of the formula) and \code{\link[=nns]{nns()}}/\code{\link[=cos_sim]{cos_sim()}}.
}
\examples{

# a toy 4 x 5 embedding matrix with covariates
set.seed(1)
E <- matrix(rnorm(20), nrow = 4)
dv <- data.frame(party = c("D", "D", "R", "R"))
my_dem <- as_dem(E, docvars = dv)
my_dem@docvars
}
\keyword{as_dem}
