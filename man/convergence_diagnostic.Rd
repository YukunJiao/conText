% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence_diagnostic.R
\name{convergence_diagnostic}
\alias{convergence_diagnostic}
\title{Diagnose how quickly an ALC embedding stabilizes with sample size}
\usage{
convergence_diagnostic(
  x,
  sizes = NULL,
  n_replicates = 20,
  confidence_level = 0.95,
  norm = "l2"
)
}
\arguments{
\item{x}{a \code{dem-class} object: the per-instance ALC embeddings of a single
focal term (e.g. from \code{\link[=dem]{dem()}}).}

\item{sizes}{(integer) vector of sub-sample sizes to evaluate. Defaults to about
ten sizes spread between a small fraction of the instances and all of them.}

\item{n_replicates}{(integer) number of random sub-samples drawn at each size.}

\item{confidence_level}{(numeric in (0,1)) confidence level for the interval
around the mean similarity at each size.}

\item{norm}{(character) \code{"l2"} for cosine similarity, \code{"none"} for inner product.}
}
\value{
a \code{data.frame} with one row per size and columns: \code{n} (sample size),
\code{value} (mean similarity to the full-sample embedding), \code{std.error}, \code{lower.ci}
and \code{upper.ci}. Plot \code{value} against \code{n} to read off the convergence curve.
}
\description{
For a focal term's per-instance ALC embeddings, assesses how many instances are
needed before the averaged ('a la carte') embedding stabilizes. For a grid of
sample sizes, the function repeatedly draws that many instances, averages them,
and measures the cosine similarity of the sub-sample embedding to the
full-sample embedding. As the sub-sample approaches the full set the similarity
approaches 1; how fast it does so indicates whether enough instances are
available -- a practical concern for rare terms, which ALC is often used for.
}
\examples{

library(quanteda)
toks <- tokens(cr_sample_corpus)
immig_toks <- tokens_context(toks, pattern = "immigration", window = 6L, verbose = FALSE)
immig_dem <- dem(dfm(immig_toks), pre_trained = cr_glove_subset,
                 transform = TRUE, transform_matrix = cr_transform, verbose = FALSE)

set.seed(2021L)
conv <- convergence_diagnostic(immig_dem, n_replicates = 10)
conv
}
\keyword{convergence_diagnostic}
