% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dem_whiten.R
\name{dem_whiten}
\alias{dem_whiten}
\title{Whiten / reduce anisotropy of a document-embedding matrix}
\usage{
dem_whiten(
  x,
  method = c("all-but-the-top", "center", "zscore"),
  n_components = NULL
)
}
\arguments{
\item{x}{a \code{dem-class} object (e.g. from \code{\link[=dem]{dem()}} or \code{\link[=as_dem]{as_dem()}}).}

\item{method}{(character) one of:
\describe{
\item{\code{"all-but-the-top"}}{(default) mean-center the embeddings and project
out the top \code{n_components} principal directions (Mu and Viswanath 2018).}
\item{\code{"center"}}{subtract the per-dimension mean only.}
\item{\code{"zscore"}}{mean-center and scale each dimension to unit variance.}
}}

\item{n_components}{(integer) for \code{method = "all-but-the-top"}, the number of
leading principal directions to remove. Defaults to \code{round(D / 100)} (with \code{D}
the number of embedding dimensions), following Mu and Viswanath (2018), and is
capped at \code{min(D - 1, N - 1)}.}
}
\value{
a \code{dem-class} object of the same dimensions, document ids and docvars
as \code{x}, with whitened embeddings.
}
\description{
Post-processes the embeddings in a \code{dem-class} object to reduce anisotropy.
Contextual embeddings (e.g. from BERT/RoBERTa/ModernBERT) and, to a lesser
extent, averaged static embeddings, tend to occupy a narrow cone in the
embedding space (Ethayarajh 2019), which inflates cosine similarities and
distorts the (squared) norms used by conText's hypothesis tests. Whitening the
embeddings before computing nearest neighbours, similarities or an embedding
regression on externally produced embeddings (see \code{\link[=as_dem]{as_dem()}} and \code{\link[=conText]{conText()}})
makes those quantities better behaved.
}
\examples{

# toy anisotropic embeddings (a shared component plus noise)
set.seed(1)
shared <- matrix(rep(rnorm(10), each = 50), nrow = 50)
E <- shared + matrix(rnorm(50 * 10, sd = 0.3), nrow = 50)
d <- as_dem(E, docvars = data.frame(g = rep(c("a", "b"), 25)))

# remove the dominant shared direction(s)
dw <- dem_whiten(d, method = "all-but-the-top")
dim(dw)
}
\references{
Mu, J. and Viswanath, P. (2018). All-but-the-Top: Simple and Effective
Postprocessing for Word Representations. ICLR.

Ethayarajh, K. (2019). How Contextual are Contextualized Word Representations?
EMNLP-IJCNLP.
}
\keyword{dem_whiten}
