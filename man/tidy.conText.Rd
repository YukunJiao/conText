% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{tidy.conText}
\alias{tidy.conText}
\title{Tidy a conText model}
\usage{
\method{tidy}{conText}(x, ...)
}
\arguments{
\item{x}{a \code{conText-class} object returned by \code{\link[=conText]{conText()}}.}

\item{...}{unused.}
}
\value{
a \code{tibble} with one row per (non-intercept) coefficient and the
columns of the model's normed-coefficient table (e.g. \code{coefficient},
\code{normed.estimate.deflated}, and, when available, \code{std.error}, \code{lower.ci},
\code{upper.ci}, \code{p.value}).
}
\description{
Returns the normed-coefficient table of a \code{\link[=conText]{conText()}} model as a tibble, for
use with the broom / tidyverse workflow.
}
\examples{

library(quanteda)
toks <- tokens(cr_sample_corpus)
set.seed(2021L)
model <- conText(immigration ~ party + gender, data = toks,
                 pre_trained = cr_glove_subset, transform = TRUE,
                 transform_matrix = cr_transform, jackknife = FALSE,
                 permute = FALSE, verbose = FALSE)
tidy(model)
}
\keyword{tidy.conText}
